<!-- Target the current release/<version> branch, not main. -->

## What

## Why

## Tests
<!-- Which tests were added or changed. Bug fixes need a regression test. -->

## Ran on
<!-- Platforms and what you did there, especially for cfg(target_os) code. -->

## Checklist
- [ ] Tests included; `cargo test` passes
- [ ] `cargo fmt` and `cargo clippy --all-targets -- -D warnings` pass
- [ ] Docs updated (`docs/`, `skills/rdc/SKILL.md` if it affects agents)
- [ ] `CHANGELOG.md` entry under Unreleased
- [ ] Commits signed off (`git commit -s`)
